/* global QUnit */
QUnit.config.autostart = false;

sap.ui.getCore().attachInit(function () {
	"use strict";

	sap.ui.require([
		"{{formatNamespace 1worklist.parameters.AppId.value}}/test/unit/AllTests"
	], function () {
		QUnit.start();
	});
});