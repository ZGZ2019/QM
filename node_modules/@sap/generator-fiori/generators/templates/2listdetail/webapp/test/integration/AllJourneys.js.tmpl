// We cannot provide stable mock data out of the template.
// If you introduce mock data, by adding .json files in your webapp/localService/mockdata folder you have to provide the following minimum data:
// * At least 3 {{2listdetail.parameters.ObjectCollection.value.name}} in the list
{{#if 2listdetail.parameters.LineItemCollection.value.name}}
// * All 3 {{2listdetail.parameters.ObjectCollection.value.name}} have at least one {{2listdetail.parameters.LineItemCollection.value.name}}
{{/if}}

sap.ui.define([
	"sap/ui/test/Opa5",
	"./arrangements/Startup",{{#if 2listdetail.parameters.FLP.value.value}}
	"./arrangements/FLP",{{/if}}
	"./ListJourney",
	"./NavigationJourney"{{#if 2listdetail.parameters.FLP.value.value}},
	"./FLPIntegrationJourney"{{/if}}
], function (Opa5, Startup{{#if 2listdetail.parameters.FLP.value.value}}, FLP{{/if}}) {
	"use strict";
	Opa5.extendConfig({
		arrangements: new Startup(),{{#if 2listdetail.parameters.FLP.value.value}}
		assertions: new FLP(),{{/if}}
		viewNamespace: "{{2listdetail.parameters.AppId.value}}.view.",
		autoWait: true
	});
});
