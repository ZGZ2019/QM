/* global QUnit */
QUnit.config.autostart = false;

sap.ui.getCore().attachInit(function () {
	"use strict";

	sap.ui.require([
		"{{formatNamespace 2listdetail.parameters.AppId.value}}/test/unit/AllTests"
	], function () {
		QUnit.start();
	});
});