sap.ui.define([
	"sap/ui/test/opaQunit",
	"./pages/App"
], function (opaTest) {
	"use strict";

	QUnit.module("Navigation");

	opaTest("Should see the app view", function (Given, When, Then) {
		// Arrangements
{{#if 3worklistodatav4.parameters.FLP.value.value}}
		Given.iStartMyFLPApp({intent : "{{3worklistodatav4.parameters.NavigationIntent}}-display"});
{{else}}
		Given.iStartMyApp();
{{/if}}

		// Assertion
		Then.onTheAppPage.iShouldSeeTheApp();

		// Cleanup
{{#if 3worklistodatav4.parameters.FLP.value.value}}
		Then.iLeaveMyFLPApp();
{{else}}
		Then.iTeardownMyApp();
{{/if}}
	});
});
